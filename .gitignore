node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example
credentials.json
token.json
.token.json
~$*
*.local

# Local-only Gemini API key (never commit)
src/lib/config.ts

# SOW documents are working papers, not repo history: they are the brief handed to whoever builds a
# thing, and they go stale the moment it is built. What survives belongs in PRODUCT_SPEC (the
# contract), §8 (why), or docs/tracking/findings.md (what we knew and left).
sow/

# The codegraph index (anatomy-cg): derived from the source, rebuilt in under a second.
.codegraph/

# Playwright
app/test-results/
app/playwright-report/

# Vercel project link (local). The repo root is linked now, not app/: Root Directory is set to `app`
# in the project settings, so a deploy runs from here and Vercel enters app/ itself. Deploying from
# inside app/ would make it look for app/app/.
.vercel
